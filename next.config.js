/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['gohired.herokuapp.com', 'images227.netlify.app', 'drive.google.com']
  }
}

module.exports = nextConfig
